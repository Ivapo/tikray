# Review record — tkr-001 (tikray)

Append-only. One heading per round, newest first. Earlier rounds are never
edited, even where a later round proves one wrong.

### Round 2 — Phase 5 only — 2026-08-18 — clean-room reviewer (resumed) — **READY**

Zero blocking, as round 1 was. Converged in two rounds, and this is the first
phase in the corpus whose **round 1 was already READY** — the difference being
that its scope was written from four decisions a person had just made, rather
than from a fresh reading of the subsystem.

**Two of the round's three targeted re-checks came back better than asked, and
one corrected me.** I had told the reviewer that Phase 7 added the filter's
standing conditions to `src/tui.rs:Browser::status`; it checked the commit and
found Phase 7 touched neither `status` nor the two constants — the hidden count
went to the footer precisely to avoid the width-contended alternatives. So the
new result-line precedence changes exactly one function and collides with
nothing shipped.

**The canonicalize corner was settled by measurement, not by reading.** The
reviewer expected it to be the round's blocker, since `realpath(3)` is widely
believed not to correct case on case-insensitive volumes. On this machine it
does: with `photo.PNG` on disk, `Path::new(".../photo.png").exists()` is `true`
and `canonicalize` returns the on-disk spelling for **both** inputs, so they
compare equal where string equality does not — same inode and dev, measured on
the same device as the repo. And it cannot fail into a *write*: that would need
`canonicalize` to succeed with different answers for one file.

Four non-blocking, all folded in after convergence. Two are worth keeping:

- **The phase had two different clearing rules for one piece of state.**
  Decision 3 said the pending confirm is "cleared by any other key"; the
  status-precedence paragraph said the result is "cleared by the next
  navigation, which is also what clears decision 3's confirm". Those sets
  differ. Split deliberately: the **confirm** clears on any key that is not the
  one that set it (tight — it is a licence to overwrite), the **result line**
  clears when the highlighted entry changes (loose — a message about a file you
  just wrote must survive the arrow key you press to go and look at it).
- **Decisions 6 and the result line could annihilate each other.** The natural
  refresh is `Browser::enter` on the current directory — which *is* the
  navigation path, so refreshing that way clears the result inside the keypress
  that produced it, and the pane says nothing about a file it just wrote. The
  two decisions were written a round apart and their interaction was unstated.
  Now: write, refresh, then set the result.

The other two: `canonicalize(a).ok() == canonicalize(b).ok()` compares
`None == None` as equal and would report `OutputIsSource` for unrelated paths —
safe direction, wrong reason, so a double failure falls through to the exists
check; and the numbered decision list had been interrupted by two unnumbered
paragraphs with decision 6 stranded below them.

### Round 1 — Phase 5 only — 2026-08-18 — clean-room reviewer with repo access — **READY**

**Round 0 (asked once for this episode).** *Does this phase produce the
observable, and is it the right one?* Yes — the converted file is §1's second
half, reached from the browsing surface, and this is the last specced phase, so
shipping it takes the spec from `partial` to `done`. Its standing is unusual and
worth recording: the phase was split out of Phase 4 by that phase's round 1 as a
sketch, and its scope was filled in eighteen days later from four questions put
to a person, none of which is derivable from §1.

Zero blocking, nine non-blocking. **The best of them was a design fault the
phase's own decision 5 had set up.** Decision 5 goes out of its way to keep the
convert keys working on a terminal that is not iTerm2 — and
`src/tui.rs:Browser::status` returns `NOT_ITERM2` *unconditionally* when previews
are off. A result line appended to that precedence would have written a file and
said nothing about it on exactly the surface where the user has no picture to
confirm it by, and the human gate item is scoped to iTerm2, so nothing would have
caught it. The result now outranks the standing conditions, stated as a *change*
to that function rather than an addition.

Two more that would have bitten:

- **`force` must not bypass `OutputIsSource`.** The second press exists to
  overwrite a *different* file; letting it through performs in place exactly the
  destructive re-encode decision 4 was written to prevent — on the second press
  of a key whose first press said the wrong thing. Ordering pinned: source, then
  exists, then write.
- **String comparison misses the case that happens.** `photo.PNG` derives
  `photo.png` — a different string naming the same file on APFS. See round 2 for
  the measurement that settled the fix.

Six smaller ones, all accepted: `Output::Jpeg`'s extension (`jpg`) was never
stated although gate item 1's literal depends on it; `Written`'s fields needed
`pub` for the same reason Phase 7's `Entry` did; `flatten` was named as a callee
though `encode` already composites on the same condition; item 4's fixture was
unnamed **and** constrained in a way that is invisible — it must not be `*.png`,
or the PNG half derives a destination equal to its source and returns
`OutputIsSource` instead of a `Written`; §2.13 had no forward pointer where this
spec's convention calls for one; and the confirm state machine is reachable only
through the human item, judged a gate weakness rather than a gap.

One incidental finding about the tooling, worth recording because it is a
property nobody had tested: a `file:symbol` citation in prose is checked against
code that **exists**, so §2.13's new note could not cite `src/tui.rs:convert_to`
and `spec-lint` failed with `CIT_SYMBOL_ABSENT`. A phase's scope table can name
its own unbuilt entry points because they are marked as additions. The note was
reworded; the linter was right.

### Phase 7 exit gate — 2026-08-17 — **PASSED**, and the gap that was closed by hand

Items 1–6 run as 14 assertions in `tests/gate_phase7.rs`, item 7 is Phases 1–4
and 6's gates byte-identical and green, and item 8 was run by a human in iTerm2.
**80 assertions green**, linter green.

**The hole item 8 declares was closed in fact, outside the gate.** Round 2 found
that nothing machine-checkable distinguishes *the indent is wired in* from *the
pure functions exist and nothing calls them*, because the indent appears only on
a tty and `cargo test` hands the binary a pipe. Driving the binary through a
pseudo-terminal at 80 columns over 1120 px — 14 px per cell, so a 28 px indent —
produced **two leading spaces** and `width=1092px`, where an unshrunk viewport
gives `1120px` (`min(1120/1200, 864/800, 1.0) = 0.933`). So both halves are
confirmed: the wrapper is called, and the shrink is real. **That harness is not
in the repo**, which is the same weaker guarantee §2.14's spike carries — its
numbers re-derive, but a second person cannot re-run it. Recorded rather than
papered over, and it is the concrete case for the pty dev-dependency the phase
declined.

**One citation went stale from this phase's own implementation.** The scope said
`a` joins `src/tui.rs:KEYS`; the count made it a function, so `KEYS` no longer
exists and `spec-lint` errored on `CIT_SYMBOL_ABSENT`. Repointed at
`src/tui.rs:keys` under §6.1's "a correction is about claims, not pointers" —
what the phase would build did not change, so `reviewed` stands. Worth noting
that the linter caught it and no human would have.

**The caps, a fourth time, and this one is funny.** The close-out predicted the
raises *and warned itself* — "by more than the diff appears to need, on this
spec's now three-for-three record of underestimating" — then underestimated both
anyway: `rules/iterm2-display.md` was raised 60 → 80 and landed at 85, and
`rules/tui.md` 105 → 125 and landed at 126. Both were raised again, to 95 and
140. The lesson has now survived being written down, which suggests the fix is
not a better prediction but not predicting: raise the cap after the prose exists.

### Round 2 — Phase 7 only — 2026-08-17 — clean-room reviewer (resumed) — **READY**

Zero blocking. Converged in two rounds, as Phase 6 did. The reviewer confirmed
decision 3 makes `tests/gate.rs:gate4_force_emits_anyway` green **deterministically**
rather than accidentally — `Command::output()` pipes the child's stdout in either
environment, so the bare-sequence branch is taken whether `cargo test` runs from
iTerm2 or headless, which is *better standing than the shipped assertion has
today*, where it held only because `window_size()` happened to resolve or not.

Five non-blocking, all folded in after convergence. Three changed the phase:

- **The hidden count moved from the list border title to the footer.** No
  collision — the title holds only `src/tui.rs:compact`'s output — but
  `compact` *exists* because that title is already tight enough to need `$HOME`
  rewritten to `~`, the pane is 35% of the window, and ratatui truncates. So
  `~/some/deep/path — 12 hidden` loses the count on exactly the narrow window
  where it matters, while the human item asserts it is visible. The footer spans
  the window and is uncontended.
- **Item 6's `+3` length assertion is gone.** It survives a later *previewable*
  fixture but not a later *refused-format* one — and refused-format fixtures are
  precisely what Phase 2 added (`still.gif`, `icon24.svgz`). Membership both
  ways for six named files, nothing about length.
- **Decision 3 now names its predicate**, `std::io::stdout().is_terminal()`,
  rather than "a piped stdout". `display` writes to `out: &mut impl Write`, which
  need not be the process's stdout; the two agree only because
  `src/main.rs:view` is the only caller.

**The finding worth keeping is the one that changed nothing.** The machine half
cannot distinguish decision 3 from *the indent was never wired in*: an
implementation shipping `indent` and `indented` as pure functions and never
calling them passes items 1–7 green, because 1–3 test the pure functions, 4
asserts the **absence** of spaces, and 5–7 are part 2 and the regression. Every
earlier phase put a property beyond the machine only when it was genuinely
unobservable — a spill, a colour, a picture. Here it is the headline change, for
a narrow reason: the indent appears only on a tty and `cargo test` gives the
binary a pipe. Item 8 now says so outright, and a pty harness is recorded as
available and not taken — the same trade Phase 2's item 4 declined when it
accepted an environment-dependent test over bundling a font.

### Round 1 — Phase 7 only — 2026-08-17 — clean-room reviewer with repo access — **NOT READY**

**Round 0 (asked once for this episode).** *Does this phase produce the
observable, and is it the right one?* Yes — part 1 changes where a drawn image
sits against the terminal edge, part 2 changes which files a person is offered to
draw. Same standing as Phase 6: reported by someone using the shipped tool.

Two blocking. **The first is the best catch of the whole corpus so far, because
it was invisible from the spec and would have failed on the author's machine
only.**

- **B1 — an unconditional indent breaks a shipped assertion, in one environment
  and not the other.** `tests/gate.rs:gate4_force_emits_anyway` runs the *binary*
  under `view --force` with stdout piped and asserts
  `out.stdout.starts_with(b"\x1b]1337;File=")`. Two leading spaces make that
  false — but `window_size()` reads `/dev/tty`, so `cargo test` from an iTerm2
  window resolves a cell size and emits them while a headless run resolves none
  and stays green. Gate item 7 forbids editing that file. Resolved as a **rule**
  rather than a workaround: the indent is for a terminal, a pipe gets the bare
  sequence, because `--force` exists so someone can capture the *sequence* and
  two spaces prepended to that byte stream are corruption. Amending
  `tests/gate.rs` was considered and **rejected** — Phase 6's evidence-versus-
  procedure distinction licensed amending a gate *script*, and applied here it
  forbids the edit. New item 4 asserts the property from Phase 7's own file.
  The reviewer also noted the phase pointed at the wrong risk: it named Phase 1's
  item 2, which asserts on `sequence` and was never exposed; item 4 was.
- **B2 — "80 assertions" where the enumeration sums to 66.** The enumeration
  (16, 11, 12, 13, 14) was right and the headline was wrong. **The same wrong
  number appears in the Phase 6 exit-gate entry above**, which is left as
  written: this record is append-only, and a correction belongs in the entry that
  found it rather than in the one that erred.

Six non-blocking, all accepted: `display` must read `src/term.rs:geometry`
**once** rather than pairing a viewport and a cell size from two reads, which is
the torn value §2.14 chose against; `Entry` needs public *fields*, not just a
public type, or the gate cannot read a label; `src/load.rs:SVG_SNIFF_LIMIT` must
become `pub` since the read happens in `src/tui.rs`; four UI placements were
unstated while a human item asserted one of them; `rules/core-pipeline.md` sits
at 100/100 and cannot be edited without a raise; and §2.6's new note described
Phase 7 in the present tense before it existed.

One was accepted as **disclosure rather than change**: `previewable` is a byte
rule, so `tests/fixtures/malformed.svg` passes it and then fails to parse. That
is §2.10's "usvg remains the final arbiter" working as designed, and it is the
honest gap between the filter's motivation and what 1024 bytes can promise.
Closing it would mean running `usvg` on every entry of every directory.

### Phase 6 exit gate — 2026-08-17 — **PASSED**, shipped before Phase 5

Items 1–5 run as 14 assertions in `tests/gate_phase6.rs`, item 6 is Phases 1–4's
gates byte-identical to `d88e0e8` and green, and item 7 was run by a human in
iTerm2 through the amended `scripts/gate-phase4.sh`. **80 assertions green**,
linter green.

**Shipped out of order, and the reason is the one §3 asks for:** Phase 6 corrects
what Phase 4 shipped the same day, Phase 5 had not started, and building 5 first
would have meant building *on top of* a grammar its own author had already asked
to change. Sequence governs planning, not shipping.

**Measured before the human ran anything**, by driving the TUI in a pty at 80×24
cells over 1280×864 px, and the placement is what makes centring checkable
without a person: `icon.svg` (24×24) placed at **row 12, column 54** and
`landscape.svg` (fitted 800×533) at **row 5, column 30**. Both reproduce the
arithmetic by hand — the icon's 24-cell horizontal shift is the part that proves
it centres rather than corner-anchors, since a vertical-only offset would look
identical on a full-width image. The dispatch was checked the same way: a bare
file emitted once and **never entered the alternate screen**, `--browse` on the
same file entered and left it.

**Nothing departed from the close-out except the caps, again.** `rules/tui.md`
went to 105 rather than the predicted single raise — it absorbed the centring
arithmetic *and* a dispatch table, and was trimmed twice before the cap was
raised at all. `rules/core-pipeline.md` needed one line trimmed to stay under the
100 it was raised to in Phase 4. The pattern across two phases now: **every
close-out that predicts a cap underestimates it**, because the prediction is made
before the prose exists.

### Round 2 — Phase 6 only — 2026-08-17 — clean-room reviewer (resumed) — **READY**

Zero blocking. Converged in two rounds. Both blockers verified resolved against
the files rather than the changelog, and the reviewer re-derived new gate item 3
end to end rather than accepting it: `fit((1200,800), Some((640,720)))` →
`(640, 427)`, which it checked against a *shipped passing assertion*
(`tests/gate_phase4.rs:gate2_fits_to_the_pane_rather_than_to_the_window`) instead
of its own arithmetic, then `ceil(427/36) = 12` → `(0, 4)`, against `ceil(800/36)
= 23 > 20` → `(0, 0)` for the native pair. It also traced item 5's three refusals
through the code and confirmed all three variants are reachable with stdout on a
pipe.

Three non-blocking, all folded in after convergence, following Phases 3 and 4's
precedent. **One is much better than its tag and is the reason this entry is
worth reading:**

- **`NotATty` and `NoScreen` share a 38-character prefix.** Both open *"stdout is
  not a terminal, so there is n…"*, so an assertion keyed on `"not a terminal"`
  passes for **either** branch — and item 5's whole stated property is that it
  fails if the dispatch is wired backwards. The item would have asserted nothing
  while looking as though it asserted everything. This is the fourth instance in
  this spec of the shape §2.8, §2.11 and §2.13 record, and the first to arrive in
  a *test* rather than in the code, which is worth noting for whoever writes the
  next gate: the failure mode does not respect the boundary. Item 5 now names the
  two divergent substrings.
- The renumbering from three decisions to four left item 4 citing "decision 3's"
  for a sentence that is now decision 4's.
- `pane_offset` returns a bare pair where its sibling returns an `Option`, and
  the spec had not said what it returns when `cell` is `None`. `(0, 0)`, now
  stated, and unobservable since no bytes are emitted in that state.

### Round 1 — Phase 6 only — 2026-08-17 — clean-room reviewer with repo access — **NOT READY**

**Round 0 (asked once for this episode).** *Does this phase produce the
observable, and is it the right one?* Yes, both halves — part 1 changes how a
user reaches an image drawn inline, part 2 changes where in its pane a drawn
image sits, and §1's observable is exactly "an image the user can see".
**And unlike Phase 4, whose round 0 recorded that §1 does not itself establish
the TUI's value, this phase comes from a person who ran the shipped tool and
reported friction** — the strongest standing a reopening can have. The caveat
recorded rather than smoothed over: part 1 reverses a decision that was argued
and rejected by name, on one person's preference after short use rather than on
measured friction. Legitimate, since it is their tool, but a later reader should
know that is the basis.

Two blocking findings on a phase drafted in one pass, and **both were arithmetic
or plumbing rather than judgement** — the phase's decisions survived round 1
intact, which is the opposite of Phases 2–4's pattern and probably reflects that
this phase reopens a subsystem already three phases deep.

- **B1 — gate item 2 asserted the literal its own prose refuted.** The item said
  a 640×433 image yields `(0, 4)`, "**not** `(0, 3)`", two clauses after deriving
  "the footprint is 13 rows and the free space 7, whose half is 3". `(0, 4)` is
  what the **floor** rule gives, and the floor is excluded by item 1, whose
  `(19, 9)` becomes `(19, 10)` under it. The reviewer brute-forced both rules
  over all five literals: no single rule satisfied the draft, so an implementer
  could not have written `centre_offset` and passed its own gate. Corrected to
  `(0, 3)`, and reframed as the one input of the five where the rules disagree —
  which is what makes it worth asserting.
- **B2 — `centre_offset`'s `image` argument was never pinned to `fit`'s output.**
  At the call site the only image in hand is the decoded buffer, so the natural
  implementation passes native dimensions; the footprint then exceeds the pane
  for essentially every real image, the offset saturates to `(0, 0)`, and **every
  picture stays in the corner with all four of item 1's assertions green**. The
  reviewer offered one clause as the fix. Taken further deliberately — a clause
  leaves the failure invisible to the gate, which is the objection §2.13 makes to
  its own silent case — so `pane_offset` now makes the wrong call unreachable and
  item 3 asserts it on a buffer whose native and fitted sizes differ.

Six non-blocking, all accepted. Two worth keeping:

- **The stated reason for rounding up was false.** The draft said the floor would
  push an image past its pane — §2.14's row 8 by a new route. The reviewer
  searched every `cell` in 1..39 against every `pane` in 1..59 and found **zero**
  such cases, because `fit` already guarantees `ceil(h/cell) ≤ pane`. The
  ceiling is still required, but by item 1's `(19, 9)` and nothing grander. The
  correction is recorded in the phase rather than quietly swapped, because a
  justification that does not hold is worse than a missing one.
- **Part 1, the phase's headline change, had no machine assertion at all** —
  items 1–2 gated arithmetic, item 3 gated parsing and said so pointedly, item 4
  was regression. The reviewer noticed the harness already exists
  (`tests/gate.rs:run_without_iterm2`) and that the three branches produce three
  *different* refusals with no terminal. Now item 5.

Two findings were answered as disclosure rather than as changes, and both stand:
a shipped test name (`gate3_a_bare_path_is_the_tui_starting_there`) goes stale
and is left stale, since renaming it edits a shipped gate file; and amending
`scripts/gate-phase4.sh` is legal because **an assertion file is evidence and a
gate script is a procedure** — procedures track the code as `rules/` files do,
and Phase 4's evidence is untouched.

### Phase 4 exit gate — 2026-08-17 — **PASSED**, and the decision taken at the keyboard

Items 1–3 and 5 run as 13 assertions in `tests/gate_phase4.rs`, item 4 is Phases
1–3's gates passing unmodified (16 + 11 + 12, byte-identical to `main`), and items
6–7 were run by a human in iTerm2 through `scripts/gate-phase4.sh`. **52
assertions green**, linter green.

**A decision the phase's own review did not cover, put to the human before
building.** Phase 4 settles what the TUI does when `cell_size()` is `None`
(decision 3) and says nothing about a terminal that is *not iTerm2*, or about
stdout not being a tty — and §2.7's check covers both in one call, so an
implementer meets them together. Settled as: **a tty is required, iTerm2 is
not.** With no terminal there is no screen to browse in, so that is a refusal;
"this is not iTerm2" costs only the preview, which is decision 3's argument
reached by a different route — the file list is useful on its own.

That refusal needed a **new error variant**, and the reason is worth keeping:
`TikrayError::NotATty`'s message ends *"redirect to a file only with --force"*,
and `--force` is `view`'s escape hatch, which buys a browser nothing. Reusing it
would have advertised a flag that does not exist on the invocation that printed
it. `NoScreen` names `view` and `convert` instead. Recorded here and in
`rules/tui.md` rather than in the spec, which is the shape the human chose.

**Measured before the human ran anything**, by driving the TUI in a pty with a
known geometry — 80×24 cells over 1280×864 px, so 16×36 per cell, §2.14's
numbers reproduced at a different size:

- `icon.svg` emitted `width=24px;height=24px` and `landscape.svg` (1200×800)
  emitted `width=800px;height=533px` into a 50×20-cell pane — the clamp holding
  at native for the icon, and `min(800/1200, 720/800, 1.0)` for the landscape;
- resizing mid-run re-emitted at `width=592px;height=395px`, which is the resize
  branch of the invalidation rule doing its job rather than being asserted;
- all three no-preview branches — not iTerm2, no pixel geometry, a directory —
  emitted **zero** OSC bytes and drew their line instead;
- the stream ends `\x07 ESC[?1049l ESC[?25h`: image, leave alternate screen,
  cursor restored, in that order.

**Three departures from the close-out, all in the caps.** `rules/convert.md` was
named "at 52/55, no raise needed" and needed one anyway (55 → 60): naming which
of `run`'s three surfaces owns §2.13's stderr note is worth four lines, and it is
the fact Phase 5 opens on. `rules/iterm2-display.md` went 50 → 60 and
`rules/core-pipeline.md` 90 → 100 as predicted. `rules/tui.md` landed at 74/75,
which is closer to its cap than Phase 3's own note advises — a rule sitting on
its cap fails the linter on the next correction, so it is the first thing to trim
when Phase 5 touches it.

### Round 3 — Phase 4 only — 2026-08-17 — clean-room reviewer (resumed) — **READY**

Zero blocking findings. Converged in three rounds — **at the cap**, and the extra
round was spent on a blocker *the round-2 fix introduced*, not on one the spec
started with. Worth recording as the loop working on itself: §7.3's warning that
"a fix can introduce a blocker" is the exact shape this episode took.

The reviewer verified the split composes rather than merely exists:
`geometry() -> Option<((u32,u32), (u16,u16))>` returns exactly `cell_size`'s two
argument types in order, so `let (px, cells) = geometry()?; cell_size(px, cells)`
needs no adapter, and the widths match crossterm's `WindowSize` on both halves —
pixels widened to `u32` as shipped `viewport` already does, cells kept `u16` as
ratatui's `Rect` and `pane_viewport` need. It re-derived every literal a third
time (2528/158 = 16, 1584/44 = 36, and multiplied back up), and confirmed round
2's `(640, 720)`, `(640, 427)` and `width=640px;height=427px` were untouched by
the round-3 diff.

Two non-blocking findings, **both folded in after convergence** rather than
deferred, following Phase 3's precedent. One is better than its tag:

- **A degenerate pane is the one path by which `auto` can still escape into the
  TUI.** With a real cell size and a pane of `(0, h)` — reachable by shrinking
  the window under a bordered layout — the multiply gives `(0, …)`,
  `src/display.rs:fit` returns `None` for a zero axis, and `sequence` emits
  `width=auto;height=auto`: §2.14's row 8 reached from the other side, defeating
  decision 3. Non-blocking only because three independent signals already point
  the right way. Gate item 2 now asserts it instead of inheriting it.
- **Item 6 did not name the spill.** §2.14's headline hazard is an image crossing
  its pane, item 5 says outright that no test can see one, so the human item was
  the only place it could be caught — and it asked for "arrow-key navigation
  redraws the image" rather than for the property. Phases 1–3 each named theirs
  specifically ("whole and undistorted inside it", "at 24 px — small", "right way
  up, right colours"); item 6 now does too. The reviewer noted it could have
  raised this in round 2 and did not.

### Round 2 — Phase 4 only — 2026-08-17 — clean-room reviewer (resumed) — **NOT READY**

All six of round 1's blockers verified resolved — several by measurement rather
than reading. The reviewer built a throwaway crate against clap 4.6.6 with the
exact derive decision 1 specifies and confirmed all five of gate item 3's cases,
including that `["tikray", "view"]` is a `MissingRequiredArgument` error rather
than a bare path named `view`, and that `./view` works as the escape hatch with
no `args_conflicts_with_subcommands` needed. It also recorded that `signal-hook`
0.3 is **already in `Cargo.lock` transitively via crossterm 0.29**, so the pin
unifies a version rather than adding a tree.

**One newly-introduced blocking finding, created by the round-1 fix.** The scope
table declared `cell_size() -> Option<(u32,u32)>` with no parameters, while gate
item 1 asserted `2528×1584 over 158×44 → (16, 36)` under a gate headed "runnable
by `cargo test` with no terminal" and said the integers were "injected". A
zero-argument function cannot receive them and cannot be pure: it must call
`window_size()`, which returns `Err` with no controlling terminal, so item 1 was
**unreachable as written** and an implementer had two incompatible instructions.

The finding's force is that Phase 1 already got this right and the phase cites it
as the reason its gates run headless — `src/term.rs:viewport` reads with no
arguments, `src/display.rs:fit` is pure with the value injected. `pane_viewport`
in the same table follows that split; only `cell_size` collapsed both roles.
Resolved by splitting it in two, with the reasoning written under the table so it
is not re-collapsed, and with **one** reader returning both pairs from **one**
`window_size()` call — two reads can straddle a resize and yield a cell size that
never existed.

Three non-blocking, all accepted: gate item 5's subject function was the only
gated function in the spec with no signature in a table (now
`src/tui.rs:pane_sequence`, with `Ok(None)` meaning *draw the explanation and
emit nothing*); the close-out missed `rules/convert.md`, whose `sources` include
`src/main.rs` and whose `covers` names `run`'s ordering — which this phase
restructures; and Phase 5's "exit gate: to be written with the phase" is against
§3, now three numbered items marked as the floor its own round raises.

The reviewer was asked directly about the one finding not fully taken (N5, the
spike leaving no repo artifact) and agreed the call was right: pulling ratatui in
for a throwaway crate ahead of the phase that adopts it buys a re-runnable table
at the cost of a dependency the repo cannot yet justify, and nothing in Phase 4's
gate rests on the spike's nine rows.

### Round 1 — Phase 4 only — 2026-08-17 — clean-room reviewer with repo access — **NOT READY**

**Round 0 (asked once for this episode).** *Does this phase produce the
observable, and is it the right one?* Yes — it draws images inside a browsing UI,
§1's observable reached through a second surface, and the phase argues that
explicitly. **But this is the first phase whose value §1 does not itself
establish, and that is recorded rather than smoothed over.** §1's stated user
wants to see an image in place and save it as another format without a GUI or two
command vocabularies — and Phases 1–3 serve that completely. The TUI is wanted
because the product was *defined* as a CLI/TUI (§1, the title, the seed), not
because the stated need demands it. Prior round 0s recorded the mirror-image
caveat — that Phases 1–2 alone duplicate `chafa`/`timg` — and Phase 3 discharged
it; nothing later discharges this one, because Phase 4 is where the spec's
original scope runs out. Not a stop, but the human was told.

Six blocking findings, on a phase that was three bullets — the same thinness that
drew four blockers on Phase 2 and six on Phase 3, and the third time the lesson
has repeated. **§2.14's factual base was verified first and held**: every
`file:symbol` it cites matches the code, and its arithmetic re-derives exactly.
Every blocker was in Phase 4's own text.

- **B1 — the only phase in the spec naming no files and no entry points.**
  Measured, not read: `src/main.rs:Cli` is a non-optional `command: Command` with
  no positional, so **`tikray` and `tikray x.png` both exit 2 today** — the two
  invocations §2.9 assigns to this phase. Restructuring the derive is a real
  decision the phase never mentioned. Resolved with a file table and three named
  decisions, including the subcommand-vs-path collision rule and `./view`.
- **B2 — a one-paragraph, entirely manual gate**, where every other phase splits
  into numbered machine items plus a human one. Replaced by five machine items,
  two human items and `scripts/gate-phase4.sh`.
- **B3 — no regression item**, against a blast radius that is the second caller
  of the core §2.2 exists for plus a crate co-owning crossterm with
  `src/term.rs`. Now item 4, all 39 assertions.
- **B4 — "a key to convert the selected file" was one ungated clause hiding a
  contradiction with §2.13.** The flattening notice is an `eprintln!` from
  `src/main.rs:run`, and Phase 3's gate item 2 asserts it — inside an alternate
  screen that writes over the TUI. **Split into Phase 5**, where *where the
  notice goes* is the decision the phase exists to make.
- **B5 — the phase opened with an OQ-1 spike already done and a cut branch that
  can no longer fire**, pointing a fresh context at discharged work as step 1.
- **B6 — the `SIGINT` clause was ambiguous between two different
  implementations.** crossterm 0.29's raw mode goes through `cfmakeraw`, which
  clears `ISIG`, so Ctrl-C inside the TUI is a `KeyEvent`; a real `kill -INT` is
  a signal that runs no `Drop`. Now two gate items and a pinned `signal-hook`.

Nine non-blocking, eight accepted: close-out too thin for its documentation
footprint; `rules/iterm2-display.md` at 50/50 unnamed; `src/term.rs:viewport`
discards `rows`/`columns` so §2.14's arithmetic has no source; §2.14 overstated
the `crossterm_0_29` feature, whose `default` already selects it; scope and gate
disagreed on what `tikray <path>` opens; §2.14's reserved no-pixel decision
appeared in neither; §2.14 omitted how the bytes are placed; and phase size.

### Phase 3 exit gate — 2026-08-16 — **PASSED**, and what the coarse rule costs

Items 1–6 run as 12 assertions in `tests/gate_phase3.rs`, item 7 is Phases 1 and
2's gates passing unmodified (16 + 11, unchanged since `03732bf`), and item 8 was
run by a human in iTerm2. **39 assertions green.** The linter is green at 13
citations. Every literal rounds 1 and 2 measured reproduced against the shipped
code, on the pinned fixtures: `[255, 127, 127]`, `[255, 255, 255]`,
`[255, 255, 243]`, `[0, 1, 0]`, `Rgb16 [65535, 1234, 7]`, `Rgba8 [65535, 1285, 0]`.

**Evidence the gate has teeth, beyond passing.** All three hazards were mutated
back in, and each failed *exactly* the assertion written for it:

- dropping `flatten` from the JPEG branch failed item 2 alone, reporting the
  transparent pixel as `[0, 1, 0]` — **item 4 stayed green**, which is the
  finding's whole point: format and dimensions cannot see this;
- `to_rgba8()` before the PNG write failed item 3 alone, `left: Rgba8,
  right: Rgb16` — again with item 4 green, since both files are `Png` at `(2, 2)`;
- routing `resolve` through `ImageFormat::from_path` failed items 1 and 6, landing
  `out.svg` on `OutputUndetermined`. Worth recording that this is *worse* than the
  crate's wrong message §2.12 rejected: it does not merely misdescribe the
  refusal, it reports the wrong refusal entirely.

**Item 8, and the observation it collected for free.** All six pairs opened in
Preview as the same picture, right way up, right colours, and drew inline through
`view`; the transparent source came out on **white**. The half worth recording is
one nobody predicted: **every SVG→JPEG conversion prints the flattening note,
transparency or not.** `src/svg.rs:rasterize` always yields `Rgba8`, and §2.13's
note deliberately fires on the buffer *having* an alpha channel rather than on any
pixel being transparent — so a fully opaque SVG announces a flattening that
changed nothing. Measured: `wide.svg` → JPEG printed it, `rgb.png` → JPEG did not.

This is the coarse rule working as written, not a defect — the decision names
being un-gettable-wrong as the reason to prefer it — but the cost was argued from
transparent sources, and it turns out to apply to *all* vector input. Whoever
finds the note noisy should weigh that rather than the transparent case. Narrowing
it means scanning every pixel, which is the precision §2.13 declined.

**Three departures from the close-out, all resolved in the code.** `TikrayError::
Encode`'s message read "could not encode the image **for display**", which the
convert edge makes false; the spec reuses the variant without noting its message
was display-specific, so the wording was widened — the same shape as Phase 2's
`FormatNotAllowed` fix, and no assertion depended on it. `tikray.md`'s Convert
bullet still promised "any of the above into any of the others", which OQ-2
narrowed, so it was corrected alongside the status line the close-out named. And
`rules/core-pipeline.md` first landed at exactly 90/90 after its cap was raised;
it was trimmed to 88/90 rather than raised again, since a rule sitting on its cap
fails the linter on the next correction.

One implementation note a later phase will want: the gate's temp output goes in
`CARGO_TARGET_TMPDIR`, cargo's own scratch space for integration tests, so items
4–6 needed no `tempfile` dev-dependency.

### Round 2 — Phase 3 only — 2026-08-16 — clean-room reviewer (resumed) — **READY**

Zero blocking findings. Converged in two rounds. All six blockers verified
resolved, four by measurement rather than by reading, and nothing was rejected so
there was nothing to re-argue.

Every literal the new gate is keyed to reproduced under the pin, and the reviewer
noted they reproduce **under one consistent fixture** — a 2×2 laid out
`[red50, clear, red50, clear]` — which is evidence they were measured together
rather than assembled from separate runs. Two checks worth recording because a
later round should not have to redo them: the naive drop-alpha bug never lifts
the transparent pixel above a min channel of **15** across nine fixture shapes,
so item 2's `> 200` threshold cannot pass a broken implementation; and
`quantized → [65535, 1285, 0]` is arithmetic, not a magic number — the stored
8-bit pixel is `[255, 5, 0, 255]`, 1234/257 = 5 and 5·257 = 1285.

Three non-blocking findings, all folded in after convergence rather than deferred.
One of them is worth more than its tag:

- **`alpha.png`'s layout was unpinned, and one plausible layout fails item 2's
  threshold on *correct* code** — measured, an 8×8 opaque red with one clear
  corner returns a min channel of 194 against a `> 200` bar. Only in the
  false-red direction, and the spec's own `[255, 255, 243]` points at the safe
  2×2 layout, but a gate item depending on a fixture nobody wrote down is one an
  implementer can reproduce as broken. Both fixtures are now pinned to an exact
  layout.
- §2.13's mandated stderr note was checked only by the human item, though it is
  trivially machine-checkable. Gate item 2 now asserts it, and §2.13 names
  `src/main.rs:run` as the emission site — `encode` is the phase's pure seam, and
  a function that writes to stderr is not one.
- Item 3's `[65535, 1285, 0]` now names the comparison space it reproduces in.

**Two questions put to the reviewer directly, both closed.** `run`'s order
(resolve → guard → load → encode → write) is consistent with the variants and
every gate item; its one visible consequence is that `convert missing.png
existing.png` reports `OutputExists` rather than `Io`, which is the deliberate
cheap-refusals-first rule and not a contradiction. And gate item 4's claim holds:
`load` accepting the written file plus Phase 1's item 2 on `sequence` covers
`view` end to end without a terminal. One looseness recorded rather than fixed —
Phase 1's item 2 exercises `sequence` on `Rgb8` while item 4's `icon24.svg` → PNG
pair re-loads as `Rgba8`; that path is shipped and was human-verified at Phase 2's
item 7, and `write_to(Png)` handles `Rgba8` and `Rgb16` natively, so no failure
mode is uncovered.

### Round 1 — Phase 3 only — 2026-08-16 — clean-room reviewer with repo access — **NOT READY**

**Round 0 (asked once for this episode).** *Does this phase produce the
observable, and is it the right one?* Yes, and emphatically — Phase 3 produces the
observable's **second half**, a written file that opens and looks right, which §1
names alongside the inline render. And it is the right one because both prior
round 0s recorded the same caveat, that Phases 1–2 in isolation duplicate
`chafa`/`timg` and this project's reason to exist lives here. This is the phase
those caveats pointed at as non-optional, so the caveat is now discharged rather
than carried forward.

Six blocking findings on a phase that was one paragraph of scope and four
sentences of gate — the same thinness that produced Phase 2's four blockers, and
the same lesson: a phase written before its open questions are answered cannot be
sized, because the answers are what it is made of.

- **B1 + B2 — OQ-2 and OQ-4 were still open**, and the phase's own scope said they
  "must be answered in the spec before this phase is cleared". OQ-2 is
  `needs-input`, so it went to a person: **SVG is input-only in v1, both readings
  refused** (§2.12). OQ-4 was answered as composite-over-white-and-say-so with
  library-default quality (§2.13). **The reviewer's measurement reframed OQ-4
  entirely**: the question offered "composite against something, or refuse", but
  `image` 0.25.10 does neither — `write_to(_, Jpeg)` on an RGBA8 buffer succeeds
  by calling `to_rgb8()`, dropping alpha with no compositing, so a transparent
  pixel lands on **black** (`[0,0,0,0]` → `[0,1,0]`). The live alternative to
  "composite" was never "refuse"; it was *silently wrong*, and it is the third
  instance of the shape §2.8 and §2.11 already record.
- **B3 — no output allowlist**, against §2.8's standing per-phase obligation.
  Measured: an RGBA8 buffer encodes cleanly to **nine** formats (Png, Jpeg, Gif,
  WebP, Bmp, Tiff, Ico, Qoi, Tga), so "whatever `image` will write" is not two.
  **Resolved structurally** — `Output` is a two-variant enum, which cannot name an
  ungated format.
- **B4 — no files, entry points or signatures, and both flags the gate was keyed
  to were unnamed.** The reviewer departed from Phase 2's round 1 (which tagged a
  missing file table non-blocking) with a reason worth keeping: there the scope
  still named `load` and its signature, whereas here a *gate item* was keyed to a
  flag no second person could reproduce. It also caught that `--force` already
  means §2.7's detection bypass, so the obvious name for an overwrite guard
  collides in the user-facing vocabulary — hence `--overwrite`, and no short forms.
- **B5 — the gate had no literal, no fixture, no named test file and no
  machine/human split**, which is Phase 2's round-1 B4 verbatim. Two clauses were
  unreachable as written: "displays as the same picture" cannot run under
  `cargo test`, and **cannot be byte-exact for JPEG** — measured, `[255,0,0]`
  round-trips as `[254,1,3]`. Replaced by seven machine items and one human item,
  with item 4 substituting "`load` accepts the written file" for the visual claim.
- **B6 — §2.1's justification for the `DynamicImage` waist was a claim about this
  gate, and this gate could not cash it.** §2.1 defends the type over RGBA8 by
  saying bit depth is "material for Phase 3, whose gate reads the converted file
  back and compares it". Measured: a faithful write reads back `Rgb16`
  `[65535,1234,7]` and a `to_rgba8()`-first write reads back `Rgba8`
  `[65535,1285,0]`, but **both report `format = Png` and `dims = (2,2)`** — so a
  gate keyed to dimensions and format passes identically under both. Item 3 now
  asserts colour type and pixel, and says why it must not assert the other two.
  This is the assertion §2.1 has been owed since Phase 1.

Five non-blocking findings, all accepted: OQ-6 was assigned to Phase 3 on a
premise Phase 3 falsifies (it adds an *output* allowlist and leaves the input one
alone, so nothing multi-frame can arrive) and is reassigned to whichever later
phase grows the input set; the close-out named neither
`rules/core-pipeline.md`'s `sources:`/`max_lines` obligation — measured at 68/70
lines — nor the README and status artifact; §2.9's invocation table showed
`convert` without the two flags its own prose promises; and the phase named no
pure seam, where Phases 1 and 2 each named one.

### Phase 2 exit gate — 2026-08-16 — **PASSED**, and what OQ-8 actually looks like

Items 1–5 run as 11 assertions in `tests/gate_phase2.rs`, item 6 is Phase 1's
`tests/gate.rs` passing unmodified (16/16, unchanged since `da2bd6a`), and item 7
was run by a human in iTerm2. The linter is green. Every literal round 2
re-derived reproduced against the shipped code: 24×24, 38×19, 10×10,
`[255, 0, 0, 128]`.

**Item 7, and the observation OQ-8 was deferred to collect.** All three fixtures
drew correctly: a 3000×600 SVG appeared whole and undistorted inside the window,
`<text>` rendered rather than blanking, and the `viewBox="0 0 24 24"` icon
appeared as a 24-pixel speck. The half worth recording is the one nobody
predicted: **the 120×40 text fixture also reads as very small** — not a speck by
construction, just an ordinary small SVG. That is the never-upscale clamp
measuring in *device* pixels (Phase 1's OQ-7 measurement) while the surrounding
terminal text is measured in points, so on a Retina Mac a 120-pixel-wide image
occupies about 60 points next to a ~14-point font.

This changes nothing about the decision and is not a defect — it is OQ-3's
resolution and §1.1's non-goal, working as written. What it changes is the
evidence: OQ-8's cost was argued from a deliberately tiny icon, where "a 24×24
input draws at 24×24" is hard to object to. It turns out to apply to any SVG
authored at document scale rather than at screen scale, which is most of them.
Whoever takes OQ-8 past v1 should weigh that rather than the icon case, and the
same is true of raster input — this was equally true in Phase 1 and simply had no
fixture that showed it.

**Evidence the gate has teeth, beyond passing.** Both hazards §2.11 names were
mutated back in:

- `take_demultiplied()` → `take()` failed **exactly one** assertion, with
  `left: [128, 0, 0, 128]`, `right: [255, 0, 0, 128]` — the value round 1
  measured. Phase 1's 16 stayed green, which is the finding's whole point: the
  corruption is invisible to anything that reads only dimensions.
- Making the SVG branch a fall-through — "if `image` cannot sniff it, hand the
  bytes to `usvg`" — failed Phase 1's
  `gate3_a_text_file_named_png_is_undetermined_not_corrupt` with
  `SvgParse(ParsingFailed(UnknownToken))`. That is §2.10's trap reproduced
  exactly: a shipped Phase 1 assertion going red for something that reads as a
  Phase 2 bug.

**One departure from the phase's close-out, resolved in the code rather than the
spec.** The close-out did not anticipate that `src/load.rs:ALLOWED` is a *cited
symbol* — §2.10 cites it — so replacing it outright broke the citation and
failed the linter. §2.10's own wording is that "`ALLOWED` becomes a check over
`Input` rather than over `[ImageFormat; 2]`", so the name survives as a
`[Input; 3]` const and the spec needed no edit. It also reads better than the
alternative: the arm that refuses is the one holding the format to name, so
`Input::Svg` needs no unreachable branch.

### Round 2 — Phase 2 only — 2026-08-16 — clean-room reviewer (resumed) — **READY**

Zero blocking findings. Converged in two rounds. All four blockers verified
resolved, three of them by measurement rather than by reading.

The reviewer implemented §2.10's byte rule exactly as written and ran it over the
repo's real fixtures plus six SVG shapes. The case the section exists for holds:
`not_an_image.png` fails the raster sniff *and* the SVG rule, so Phase 1's shipped
gate item 3 survives at the new seam. Bare `<svg>`, XML prolog, BOM, leading
whitespace, uppercase `<SVG` and a `<!DOCTYPE svg>` preamble all detect; `.svgz`
does not, matching the named non-goal.

Every literal the new gate is keyed to reproduced under the pin: 24×24, 38×19,
10×10, `[255, 0, 0, 128]`, and 16 tests in `tests/gate.rs`. **One check worth
recording because it is the kind a later round should not have to redo:**
`to_int_size()` was confirmed to *round* rather than ceil (10.2→10, 10.5→11,
10.7→11). A gate keyed to 38×19 alone would have passed under either mode, so
§2.11 names the mode the method actually uses rather than relying on a literal
that cannot distinguish them. The demultiplied pixel was also confirmed identical
under `fill-opacity` and `opacity`, so gate item 3's fixture wording cannot flip
it. Gate item 6 was checked for *satisfiability*, not just correctness: the
shipped test file touches only `load`, `fit`, `sequence` and two `{ .. }` error
patterns, all of which absorb the `ALLOWED`-over-`Input` change.

Four non-blocking findings, all folded in rather than deferred:

- `rasterize(bytes)` could not construct either error variant the same table gave
  it — both carry a `path` the signature never received. Resolved by passing the
  path for labelling only, following `TikrayError::io`'s precedent; the function
  stays a deterministic function of `bytes`.
- `FormatNotAllowed`'s message ends "supported input formats are PNG and JPEG",
  which the allowlist growing makes false. Named in the close-out, with the note
  that `tests/gate.rs` needs no edit for it — the shipped assertion checks only
  that the message names GIF — which is what keeps gate item 6 satisfiable.
- Gate item 4 is environment-dependent (it needs system fonts) — the property
  Phase 1's item 4 was reshaped to avoid. Accepted with the reason stated in
  place, since bundling a font is a heavier decision than the assertion is worth.
- §2.6's first `CORRECTED` note had gone stale in one clause, still claiming
  Phase 2 needs a viewport to rasterize against, which §2.11 removed.

**On OQ-8, asked directly:** the reviewer judged the clamp does not make the
observable not worth producing — an over-large SVG now draws whole and sharp,
which is a real new capability, and the 24px speck is argued from OQ-3 and §1.1
rather than defaulted into. Round 0 was not reopened.

### Round 1 — Phase 2 only — 2026-08-16 — clean-room reviewer with repo access — **NOT READY**

**Round 0 (asked once for this episode).** *Does this phase produce the
observable, and is it the right one?* Yes — Phase 2 draws an SVG inline, the same
observable Phase 1 produces, reached from vector input; and it is the right next
one because §2.1's whole claim is that `resvg` yields a pixel buffer, making SVG
indistinguishable from PNG downstream, which this phase is what cashes. Caveat
carried forward from Phase 1's round 0: the project's reason to exist lives in
Phase 3, so Phase 2 puts a second phase between shipped work and the
differentiating capability — an argument for keeping Phase 3 non-optional, not
against building Phase 2. Nothing Phase 1 measured argued for reordering.

Four blocking findings. The phase as drafted was one paragraph of scope and two
sentences of gate, and every blocker came from that thinness.

- **B1 + B2 — the scope made two claims that could not both hold, and the second
  was vacuous anyway.** "Behind the same `load(path) -> DynamicImage` entry
  point" and "rasterization target size derives from the display sizing decision"
  are jointly unsatisfiable: `load` takes no viewport, and the viewport is not
  discovered until `display` calls `viewport()`, two steps later in `run`. And
  the sizing derivation buys nothing regardless, because §2.6's `scale` never
  exceeds `1.0` — measured: a `viewBox="0 0 24 24"` icon draws as 24 device
  pixels either way, so the seed's "the one place where output quality actually
  depends on getting sizing right first" is false under the rule §2.6 records.
  **Resolved** by new §2.11: the claim is deleted, Phase 2 rasterizes at usvg's
  resolved size, and `load`'s signature does not change.
- **B3 — SVG is undetectable by the mechanism §2.8 pins, and the obvious fix
  breaks a shipped assertion.** Measured: `with_guessed_format()` returns `None`
  for plain, XML-prolog, BOM-prefixed and gzipped SVG alike, and
  `image::ImageFormat` has no SVG variant, so neither `ALLOWED` nor
  `FormatNotAllowed { format: ImageFormat }` can express one. The trap is sharper
  than the gap: SVG bytes produce `FormatUndetermined`, *the identical outcome
  Phase 1's gate item 3 asserts for a text file named `.png`*, so "if `image`
  cannot sniff it, hand it to usvg" turns that shipped assertion into an SVG
  parse error. **Resolved** by new §2.10 — an `Input` enum, a positive three-step
  dispatch, and the SVG rule stated in bytes.
- **B4 — the gate had no machine-checkable assertion and no literal**, on a phase
  that modifies the shared entry point. Its "not a blank image" clause also named
  a state the code cannot reach, since `run` returns before `display` writes.
  **Resolved** by six machine items plus one human item, keyed to real literals,
  with item 6 requiring Phase 1's 16 assertions to pass unmodified.

Eleven non-blocking findings, all accepted. The two worth naming because they are
silent-corruption shaped, the same class as round 2's B3 on §2.8:
`tiny_skia::Pixmap` is **premultiplied**, so `RgbaImage::from_raw(w, h,
pixmap.take())` carries 50%-opacity red through as `[128, 0, 0, 128]` and
composites it over white as `(191,127,127)` instead of `(255,127,127)`; and
`usvg::Options::default()` has an **empty font database**, so `<text>` renders
zero pixels with no error — precisely the blank image the gate forbids, on the
branch it did not cover. Both are now gate items. Also accepted: the
`resvg`/`usvg` pin §2.4 had deferred to this phase and this phase had not made
(0.48.1, with `tiny-skia` 0.12.0, compiled clean with no C toolchain);
§2.6's parenthetical "an SVG has no inherent size to rasterize at" being simply
false, since usvg resolves one four ways; the missing file table; the unstated
per-phase allowlist; fractional sizes needing a rounding rule; `resources_dir`
dropping relative raster hrefs; and `rules/core-pipeline.md` needing a `sources:`
and `max_lines` change, not just prose.

### Phase 1 exit gate — 2026-08-16 — **PASSED**, and gate item 5's two measurements

Items 1–4 run as 16 assertions in `tests/gate.rs`; items 1–4 and the linter are
green, and item 5 was run by a human in iTerm2 on a Retina Mac.

**Measurement 1 — `window_size()` reports real pixels here.** The `auto` fallback
is *not* the primary path. A 64×48 source emitted
`inline=1;width=64px;height=48px;preserveAspectRatio=1`. §2.6's warning to
"assume the fallback is a live path" did not fire, and §2.6 carries a dated
`CORRECTED` note saying so, because a Phase 2 implementer planning around the
`auto` branch would be planning around the uncommon case. The branch stays
reachable and tested: any process without a controlling terminal takes it,
including `cargo test`'s own `--force` run.

**Measurement 2 — the viewport is in device pixels, not display points. OQ-7
resolves negatively.** An image wider than the window occupied approximately the
full window width, not about half. No divisor is needed in §2.6 and Phase 2 does
not inherit the problem. Recorded as the round-3 reviewer asked, having been
unable to settle it themselves for want of a controlling terminal.

Item 5's visual half also confirmed: PNG and JPEG each drew the correct picture,
an over-wide image appeared whole and undistorted, and a 16×16 icon appeared
small rather than blown up to fill the window — the never-upscale rule holding in
the terminal and not only in the arithmetic.

**Evidence the gate has teeth, beyond passing.** `load` was mutated to §2.8's
row-2 construction — `ImageReader::open(p)?.with_guessed_format()?` — and
exactly one assertion failed, `gate3_a_text_file_named_png_is_undetermined_not_corrupt`,
with `Decoding(... Png ... InvalidSignature)`: the same "corrupt PNG" outcome
round 2's B3 finding measured. So gate item 3's asserted distinction is real
externally visible evidence of which construction shipped, rather than an
assertion that happens to pass.

Two deliberate departures from the phase's file table, both argued from the spec
rather than convenience: `src/lib.rs` exists because §2.2 makes the library the
product and items 1–3 can only reach `fit`/`sequence`/`load` through a lib
target; `README.md` exists because Phase 1 ships user-facing CLI behaviour and
§6's close-out hook wants documentation or a logged gap.

### Round 3 — Phase 1 only — 2026-08-16 — clean-room reviewer (resumed) — **READY**

Zero blocking findings. Converged inside the §7.6 three-round cap.

B3 verified resolved **by the method that found it** — all three constructions in
§2.8's table compiled against `image` 0.25.10 and run over five fixtures. Every
cell of the table reproduced, including both quoted error strings. Row 2's
degradation is *silent*: the PNG-named-`.txt` case succeeds under it, which is
why the trap hides. Row 3 produces exactly the taxonomy §2.8 claims, so gate
item 3's asserted distinction is real and is externally visible evidence of
which construction shipped.

Two non-blocking findings, both folded in rather than deferred:

- §2.6's "assume the fallback is a live path" and gate item 4's rationale
  asserted opposite things about the same machine. Item 4's rationale is now
  conditional; its point was only that two environments can differ, and item 5
  is what actually measures this one.
- **Retina points-vs-pixels** — §2.6 compares image pixels against a viewport
  whose units may be display points. If so, every image renders at ~half size
  on a Retina Mac **and passes every machine-checkable item in the gate**: still
  whole, still undistorted, still never upscaled. Recorded as **OQ-7**, with a
  clause added to gate item 5 (an over-wide image should occupy approximately
  the full window width, not half) — the only place it is catchable. The
  reviewer could not settle it: this agent shell has no controlling terminal, so
  `/dev/tty` was unavailable and the ioctl could not be run.

### Round 2 — Phase 1 only — 2026-08-16 — clean-room reviewer (resumed) — **NOT READY**

One new blocking finding, and the best catch of the review because it came from
compiling rather than reading:

- **B3 — §2.8's stated error taxonomy was unreachable through the mechanism
  §2.8 named.** `ImageReader::open(p)?.with_guessed_format()?` reads as content
  detection, but `open` seeds the format from the path extension and
  `with_guessed_format` is `self.format = format.or(self.format)` — a failed
  guess keeps the extension's answer. Measured: a text file named `.png` was
  refused as `Format error decoding Png: Invalid PNG signature`, i.e. as a
  corrupt PNG, which is precisely the outcome §2.8 promised it would not be and
  gate item 3 asserted against. The likely failure mode was an implementer
  weakening the gate to match the library. **Resolved** by naming
  `ImageReader::new(BufReader::new(File::open(p)?)).with_guessed_format()?` and
  rewriting §2.8 around a three-row table of all three constructions, with the
  trap marked as a trap.

The five `fit` literals were re-derived independently and all reproduce. The
author's own in-flight correction was confirmed: `(3,3)` into `(1,100)` gives
`scale = 1/3` and `round(1.0) = 1`, so the `max(1, …)` floor never fires; the
replacement `(10,3)` into `(1,100)` computes `round(0.3) = 0` and does exercise
it. No case sits on a rounding tie, so the literals are independent of the
rounding mode. Version pins confirmed current, not merely existing: `image`
0.25.10, `base64` 0.23.1, `clap` 4.6.6, `crossterm` 0.29.0.

Three non-blocking findings, all accepted: §2.6's zero-check was justified by a
hedge when crossterm's documentation states it outright ("may not be reliably
implemented or default to 0"; unix *unused*; Windows not implemented);
`window_size()` reads `/dev/tty` rather than stdout, so gate item 4's `--force`
assertion is now prefix-only; and Phase 2's prose still said "RGBA buffer" after
the N7 fix.

### Round 1 — Phase 1 only — 2026-08-16 — clean-room reviewer with repo access — **NOT READY**

**Round 0 (asked once for this episode, not re-litigated after).** *Does this
phase produce the observable, and is it the right one?* Yes — Phase 1 draws a
PNG or JPEG inline in iTerm2, which is literally the first half of the recorded
observable, so it produces one rather than having to argue it does not; and it
is the right first one because it is the shortest path from no code to anything
visible, and it retires the project's largest unknown (does the inline protocol
display what we emit, at a sane size, in this terminal) before SVG, conversion
or a TUI are stacked on it. Recorded caveat: **Phase 1 in isolation duplicates
`chafa`/`timg`** — this project's reason to exist lives in Phase 3, so a Phase 1
that shipped alone would be a nicer-packaged version of a tool the user already
has. That is an argument for keeping Phase 3 non-optional, not against building
Phase 1 first.

The repo had no code at this round, so there were zero `file:symbol` citations
to check and grounding rested entirely on external facts. Those were verified
against primary sources (iTerm2's Inline Images Protocol page, `docs.rs/image`).

Two blocking findings:

- **B1 — the display path as specified would have downloaded the file instead of
  showing it.** §2.3 described "print the escape sequence" while never
  enumerating a single `File=` argument, and `inline` **defaults to 0**: iTerm2's
  documentation says the file "will be downloaded with no visual representation
  in the terminal session." Implemented from the spec alone, that is a silent
  download, exit 0, nothing displayed — exactly the failure §1 names when it
  refuses to treat the escape sequence as the observable. The spec identified the
  hazard and then omitted the one argument preventing it. **Resolved** in §2.3 by
  making `inline=1` mandatory, pinning the full argument string, and asserting the
  byte in gate item 2.
- **B2 — the gate's "outside iTerm2 → exit non-zero" clause named no detection
  mechanism, and the candidates disagree.** Four inconsistent environment signals
  plus iTerm2's own Feature Reporting protocol, differing in behaviour (only
  `LC_TERMINAL` survives ssh; none survive plain tmux; WezTerm implements the
  protocol but is not iTerm2) and by an order of magnitude in cost — which also
  changed whether the phase was one plan-mode pass. **Resolved** by new §2.7:
  tty check plus `TERM_PROGRAM`/`LC_TERMINAL`, Feature Reporting recorded as
  rejected *with its trade-off written down* rather than silently omitted, and
  `--force` as the escape hatch for the known false negatives.

Nine non-blocking findings, all accepted: the gate had nothing machine-checkable
and its sizing clause was self-certifying (the implementer chose the default it
was checked against, so it could not fail); "fits the window" was satisfiable by
upscaling a 16×16 icon to fill the screen; OQ-3's option list contradicted the
gate meant to resolve it; §2.3 never mentioned the protocol's sizing arguments
exist; the supported-input allowlist was unstated while §2.4 assigned that duty
to a gate that did not discharge it; extension-vs-content detection was Phase 1's
choice with Phase 2 already constraining it; the buffer type was unnamed and
§2.1's "RGBA" contradicted §2.3's "no colour-approximation anywhere"; the phase
named no files, signatures or dependency versions, against conventions §3; the
JPEG→RGBA→PNG round-trip was presented as forced when it is chosen; OQ-5 named
no phase, against conventions §4; and §2.4 overstated WebP (encode is
lossless-only).

**Notable adjudication — a suggestion resolved differently.** The reviewer
proposed delegating sizing to the terminal with `width=100%;height=100%`,
observing it would shrink the phase measurably. Rejected in favour of computing
`px` in §2.6, because *filling is not fitting*: the protocol fills "as much as
possible without stretching", which upscales, and a 16×16 favicon would fill the
window as a screenful of blurred squares. Round 2 agreed on re-review — the
suggestion would have reintroduced exactly the case gate item 5 now names.

**Numbers this round's fixes introduced**, re-derived at round 2 and recorded so
a later round can trust them: `fit` maps `(800,600)/(1600,1200) → (800,600)`,
`(4000,1000)/(1000,1000) → (1000,250)`, `(10,3)/(1,100) → (1,1)`, and unreported
or zero-axis viewports → `None`.
